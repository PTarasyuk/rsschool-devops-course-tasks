# Terraform Infrastructure

This directory contains all Terraform code related to the AWS infrastructure for the course.

## Structure

- `base/`: Defines base infrastructure, including backend setup and IAM role for GitHub Actions.
- `main/`: Contains the main infrastructure configuration (VPC, subnets, security groups, etc.).
